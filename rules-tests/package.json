{
  "name": "respondcrew-firestore-rules-tests",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --only firestore --project demo-respondcrew \"node --test firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
